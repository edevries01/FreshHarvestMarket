@*
 * Discount/Add/Edit.cshtml
 * FreshHarvestMarket
 *
 * This Razor view provides a form for creating or editing discounts.
 *
 * It is used by administrators to:
 * - Assign a discount percentage to a produce item
 * - Edit existing discount values
 *
 * The form is populated with a list of available Produce items
 * & binds a Discount model using ASP.NET Core model binding.
 *
 * Client-side validation is enabled to ensure valid discount input.
 *@

@*Screen for adding/editing a discount*@
@model Discount

@{
    ViewData["Title"] = ViewBag.Action == "Add"
        ? "Add Discount"
        : "Edit Discount";
}

<h2 class="mb-3">
    @(ViewBag.Action == "Add" ? "Add a New Discount" : "Edit Discount")
</h2>

<form asp-action="Edit" method="post">

    <div asp-validation-summary="All" class="text-danger mb-3"></div>

    <!-- PRODUCE -->
    <div class="mb-3">
        <label asp-for="ProduceId" class="form-label">Produce</label>
        <select asp-for="ProduceId" class="form-select">
            <option value="">-- Select a product --</option>
            @foreach (Produce p in ViewBag.Produce)
            {
                <option value="@p.ProduceId">@p.ProduceName</option>
            }
        </select>
        <span asp-validation-for="ProduceId" class="text-danger"></span>
    </div>

    <!-- DISCOUNT -->
    <div class="mb-3">
        <label asp-for="DiscountAmount" class="form-label">Discount (%)</label>
        <input asp-for="DiscountAmount" class="form-control" placeholder="Enter % (1–99)" />
        <span asp-validation-for="DiscountAmount" class="text-danger"></span>
    </div>

    <input type="hidden" asp-for="DiscountId" />

    <button type="submit" class="btn btn-success">Save</button>
    <a asp-controller="Discount" asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}